import HeightGet, { HeightGetProps } from './Get';
import HeightSet, { HeightSetProps } from './Set';

const Height = {
  Get: HeightGet,
  Set: HeightSet
};

export default Height;
export { HeightGet, HeightSet };
export type { HeightGetProps, HeightSetProps };
