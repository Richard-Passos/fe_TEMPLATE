import { ScrollAnimateConfigOptions } from '@/components/atoms/ScrollAnimate';

type yFullOptions = ScrollAnimateConfigOptions;

const yFull: yFullOptions = { prop: 'y', propPoints: ['-100%', '100%'] };

export default yFull;
export type { yFullOptions };
