import ErrorTemplate from './Error';
import LegalTemplate from './Legal';
import PageTemplate from './Page';
import SingleProjectTemplate from './SingleProject';

const Templates = {
  Error: ErrorTemplate,
  Legal: LegalTemplate,
  Page: PageTemplate,
  SingleProject: SingleProjectTemplate
};

export default Templates;
export { ErrorTemplate, LegalTemplate, PageTemplate, SingleProjectTemplate };
