import cookiesConsent from './cookiesConsent';
import footer from './footer';
import header from './header';
import pages from './pages';
import personal from './personal';

const pt = {
  cookiesConsent,
  footer,
  header,
  pages,
  personal
};

export default pt;
export { cookiesConsent, footer, header, pages, personal };
