import Slot, {
  SlotUtilAsChildProps as AsChildProps,
  SlotUtilProps as SlotProps
} from './Root';
import Slottable, { SlottableProps } from './Slottable';

export default Slot;
export { Slottable };
export type { SlotProps, AsChildProps, SlottableProps };
