import CatalogFilterButton from './Button';
import CatalogFilterSelect from './Select';

const CatalogFilter = {
  Button: CatalogFilterButton,
  Select: CatalogFilterSelect
};

export default CatalogFilter;
export { CatalogFilterButton, CatalogFilterSelect };
