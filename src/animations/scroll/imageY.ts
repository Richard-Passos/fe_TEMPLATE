import { ScrollAnimateConfigOptions } from '@/components/atoms/ScrollAnimate';

type imageYOptions = ScrollAnimateConfigOptions;

const imageY: imageYOptions = {
  prop: 'y',
  propPoints: ['-13%', '0%']
};

export default imageY;
export type { imageYOptions };
